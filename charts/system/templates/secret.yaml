{{- if eq .Values.space.token "" }}
{{- fail "A Values.token is required" }}
{{- end }}

apiVersion: v1  
kind: Secret
metadata:
  name: {{ include "system.fullname" . }}
data:
  {{- if .Values.hub.url }}
  CZ_HUB_URL: {{ .Values.hub.url | b64enc }}
  {{- end }}
  CZ_HUB_SPACE_TOKEN: {{ .Values.space.token | b64enc }}
---
apiVersion: v1
kind: Secret
metadata:
  name: space-cert
data: {}
