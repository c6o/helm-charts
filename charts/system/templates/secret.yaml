{{- if eq .Values.token "" }}
{{- fail "A Values.token is required" }}
{{- end }}

apiVersion: v1  
kind: Secret
metadata:
  name: {{ include "system.fullname" . }}
data:
  {{- if .Values.hubAPI }}
  CZ_HUB_API: {{ .Values.hubAPI | b64enc }}
  {{- end }}
  CZ_HUB_SPACE_TOKEN: {{ .Values.token | b64enc }}